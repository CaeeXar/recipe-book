import { ResponsiveColsDirective } from './responsive-cols-directive.directive';

describe('ResponsiveColsDirective', () => {
  it('should create an instance', () => {
    const directive = new ResponsiveColsDirective();
    expect(directive).toBeTruthy();
  });
});
